var fullName = "Rahul Kumar";

// how to name variables

// fullName - Most preferrable, Camel Case
// full-name - better if ignored but can be used sometimes
// full_name - Snake case - can be used 
// FullName - Totally ignore

var gitName = "Regal Rahul";

var isLoggedIn = true; // boolean true/false
// False/True is not considered boolean both are different.

var loggedCount = 23;

var paymentMode; // undefinded, data can be defined later on
console.log(paymentMode); // now undefined
paymentMode = "Credit Card" // data modified here
console.log(paymentMode); // now prints the modified or inserted data

console.log(loggedCount); // prints variable data
console.log("loggedCount"); // prints passed string inside " xyz "

// var if = 24; Resered keywords not allowed. editor will make you aware make sure to use a good editor.